<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clockapp">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_clock"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_clock"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClockApp">
        <activity android:name=".ActivitySetting"></activity>
        <activity android:name=".ActivityVibrateMode" />
        <activity android:name=".ActivityRingtoneSound" />
        <activity android:name=".ActivityPauseMode" />
        <activity android:name=".ActivitySetAlarm" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".service.AlarmBroadcastReceiver"/>
        <receiver android:name=".service.BrStopAlarmService"/>
        <receiver android:name=".service.BrStopCountService"/>

        <service android:name=".service.AlarmService"/>
        <service android:name=".service.ServiceCountTime"/>
    </application>

</manifest>